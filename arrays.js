var chocolateBars = [
  'snickers',
  'hundred grand',
  'kitkat',
  'skittles',
  ]
  
function addElementToBeginningOfArray(array, element) {
  var array1 = ['${array}']
  const newArray = array1.unshift('${b}')
  return newArray
}